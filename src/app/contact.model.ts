export interface Contact { // define the model and the export mean that we can use it in other files.
    id: string;   // id:number;
    name:string;
    email:string;
    phone:string;
    address:string;
}